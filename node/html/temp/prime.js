onmessage = function (e) {
    let checked = parseInt(e.data.num);
    
    if(num)
    for(let i = 2; num > i; i++ ) {
        if(num % i == 0) {
            false;
        }
        else 
    }




    // for (let i = from; i <= to; i++)
    //     sum += i;
    postMessage(checked);
}
