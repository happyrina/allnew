apiVersion: v1
kind: Service
metadata:
  name: weathers
  labels:
    run: weathers
spec:
  type: NodePort
  ports:
  - nodePort: 31200
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: weathers
  type: LoadBalancer
  externalIPs:
  - 3.37.204.255
